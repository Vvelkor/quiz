'use client'; import {useEffect,useState} from 'react'; export default function Play(){const[d,setD]=useState<any>(null);const[i,setI]=useState(0);const[a,setA]=useState('');const[s,setS]=useState(0);useEffect(()=>{fetch('/api/quiz/generate',{method:'POST'}).then(r=>r.json()).then(setD)},[]); if(!d)return <main style={{padding:40}}>Chargement...</main>; const q=d.questions[i]; return <main style={{padding:40}}><h2>Score {s}</h2><p>{q.question}</p><input value={a} onChange={e=>setA(e.target.value)} /><button onClick={()=>{if(a.toLowerCase()==q.answer.toLowerCase())setS(s+10);setA(''); if(i<d.questions.length-1)setI(i+1); else alert('Score '+s)}}>Valider</button></main>}